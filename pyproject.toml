[project]
name = "contacts"
version = "0.1.0"
description = "Contacts API — OpenAlex search graph"
requires-python = "==3.12.*"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "httpx>=0.27.0",
    "pydantic-settings>=2.6.0",
]

[tool.vercel]
entrypoint = "app.main:app"
